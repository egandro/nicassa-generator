export class Configuration {
    public static ServerWithApiUrl: string = 'http://localhost:1234/basepath/with/slash/';
    public static ProxyServerUrl: string = '';
    public static Timeout: number = -1;
    public static strictSSL: boolean = true;

    public url(): string {
        return Configuration.ServerWithApiUrl;
    }

    public proxyUrl(): string {
        return Configuration.ProxyServerUrl;
    }

    public timeout(): number {
        return Configuration.Timeout;
    }

    public strictSSL(): boolean {
        return Configuration.strictSSL;
    }

    public handleError(err: any) {
        console.error(response);
    }
}
